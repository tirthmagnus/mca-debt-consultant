"use client";
import { useState } from "react";
const states={pressure:{label:"Current pressure example",withdrawal:58,operations:42,note:"Frequent withdrawals consume a larger share of available operating cash."},adjusted:{label:"Illustrative lower-withdrawal example",withdrawal:32,operations:68,note:"A lower withdrawal share could leave more room for operating priorities. Example only."}};
export default function PressureScenario(){const[mode,setMode]=useState<keyof typeof states>("pressure");const s=states[mode];return <div className="dashboard"><div className="dashTop"><div><span>ILLUSTRATIVE CASH-FLOW VIEW</span><strong>{s.label}</strong></div><div className="toggle"><button onClick={()=>setMode("pressure")} className={mode==="pressure"?"on":""}>Pressure</button><button onClick={()=>setMode("adjusted")} className={mode==="adjusted"?"on":""}>Lower withdrawal</button></div></div><div className="bars"><div><div className="barLabel"><span>MCA withdrawals</span><strong>{s.withdrawal}%</strong></div><i><b style={{width:`${s.withdrawal}%`}}/></i></div><div><div className="barLabel"><span>Operating cash remaining</span><strong>{s.operations}%</strong></div><i><b style={{width:`${s.operations}%`}}/></i></div></div><div className="flow"><span>Revenue arrives</span><b>→</b><span className="danger">Withdrawal cadence</span><b>→</b><span>Payroll / inventory / rent</span></div><p>{s.note}</p><small>Illustrative percentages for interface demonstration only. Not a prediction, quote, or promised outcome.</small></div>}
